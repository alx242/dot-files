Some useful apps missing in Linux. Accessed using Google Chrome.

Add these as extensions (chrome://extensions) and they will become
proper google apps (chrome://apps).

When they are launched they will become self-contained and can be
added as proper icons on the desktop.